<!-- Generated by ADAS-CC. Edit freely; regenerate with `adas-cc scan` -->

# A python (FastAPI) project using pytest for testing.

## Key facts (from automated scan)
- Package managers: pip/poetry/uv
- CI: yes (GitHub Actions)
- Entry points: app/main.py

## Conventions
Follow the existing code style in the repository. Prefer small, reviewable diffs.
Run `pytest` before considering a task complete. Use `ruff format` for formatting
and `ruff check` for linting — both are wired into the `format-changed-file.sh` hook.

## Agent system
This repo's `.claude/` directory was generated by ADAS-CC. Four subagents cover
the standard flow: `planner` -> `implementer` -> `reviewer` -> `tester`.
Use `/plan` to kick off a new task, or `@planner`, `@implementer`, `@reviewer`,
`@tester` to invoke one directly.
