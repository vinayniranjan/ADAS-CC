<!-- Generated by ADAS-CC. Edit freely; regenerate with `adas-cc scan` -->

# A typescript (React) project using Vitest for testing.

## Key facts (from automated scan)
- Package managers: npm
- CI: yes (GitHub Actions)
- Entry points: src/main.tsx

## Conventions
Components live under `src/components/`, one component per file, colocated
with its `.test.tsx`. Use functional components and hooks — no class
components. Run `npm run lint` and `npm test` before considering a task
complete.

## Agent system
This repo's `.claude/` directory was generated by ADAS-CC. Four subagents
cover the standard flow: `planner` -> `implementer` -> `reviewer` -> `tester`.
