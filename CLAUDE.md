<!-- Generated by ADAS-CC. Edit freely; regenerate with `adas-cc scan` -->

# A python project (no specific framework detected) using pytest for testing.

# Project Overview

This is a **python** project. Frameworks: no specific framework detected. Test tooling: pytest.

## Key facts (from automated scan)
- Package managers: pip, pip/poetry/uv
- CI: not detected
- Entry points: n/a

## Conventions
Follow the existing code style in the repository. Prefer small, reviewable diffs.
Run the project's test suite before considering a task complete.

## Agent system
This repo's `.claude/` directory was generated by ADAS-CC. Four subagents cover
the standard flow: `planner` -> `implementer` -> `reviewer` -> `tester`.
