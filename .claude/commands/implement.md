---
description: Write or refactor Python source code in src/ based on a plan or description
argument-hint: [task description or plan reference]
allowed-tools: Read, Write, Edit, MultiEdit, Glob, Grep, Bash
---

Use the implementer agent to write or edit Python source code for the following task. Follow all src-layout conventions, add type hints and Google-style docstrings, and run ruff/mypy before finishing.

$ARGUMENTS
